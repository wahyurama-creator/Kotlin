package basic_kotlin

fun main() {
    val result: Int = 10 / 2
    println(result)

    var total = 0
    // variable muttable atau tidak bisa dirubah

    val harga1 = 100
    total += harga1

    val harga2 = 200
    total += harga2

    println(total)
}